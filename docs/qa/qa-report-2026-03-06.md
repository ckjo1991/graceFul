# GraceFul QA Audit Report
Date: 2026-03-06

## Summary
| Area | Status | Notes |
|---|---|---|
| Discovery (Step 0) | DRIFT | Undocumented components exist; no flow-state drift; no direct Supabase UI calls; untested lib files exist outside completed-task coverage; translations live in `lib/translation.ts` |
| Build | PASS | `npm run build` completed successfully |
| Tests | PASS | 47 passed, 0 failed |
| Guardian Integrity | PASS | All Guardian integrity checks passed |
| Debug Artifacts | FAIL | `console.log` remains in runtime code |
| TypeScript Surface | FAIL | Realtime handlers still use `: any` |
| Translation Coverage | FAIL | `hil` and `es` are declared supported but missing dedicated UI copy entries |
| Principle Violations | REVIEW | User-facing `account` copy found; no social-metric matches flagged |
| Realtime Safety | FAIL | Null guards exist, but post UPDATE syncing does not verify a `hearts` field |
| Dev Tooling Gate | FAIL | `TestDashboard` self-gates internally but is imported and rendered unconditionally in `/feed` |

## Discovery Findings (Step 0)

### D1 — Flow State Drift
- Documented in `claude.md`: `feed`, `emotion`, `category`, `message`, `crisis`, `warning`, `support`, `review`, `done`
- Actual in code: `feed`, `emotion`, `category`, `message`, `crisis`, `warning`, `support`, `review`, `done`
- Result: no drift

### D2 — Undocumented Components
- `components/ShareStepShell.tsx`
- `components/MessageStep.tsx`
- `components/CrisisScreen.tsx`
- `components/CategoryStep.tsx`
- `components/ReviewStep.tsx`
- `components/SupportStep.tsx`
- `components/GuardianWarning.tsx`
- `components/ui/textarea.tsx`
- `components/ui/button.tsx`
- `components/ui/badge.tsx`
- `components/ui/card.tsx`
- `components/reference/prayer-feed.reference.tsx`
- `components/reference/share-flow.reference.tsx`

### D3 — Direct Supabase Calls
- No `supabase.from(` or `createClient(` matches found under `app/` or `components/`

### D4 — Untested Lib Files
- No gaps flagged against completed testing tasks in `tasks.md`
- Lib files without same-name companion test files:
  - `lib/db.ts`
  - `lib/supabase.ts`
  - `lib/ai.ts`
  - `lib/constants.ts`
  - `lib/testData.ts`
  - `lib/nudge-engine.ts`
  - `lib/utils.ts`
  - `lib/translation.ts`
  - `lib/sample-posts.ts`
- Completed-task coverage that does exist:
  - `lib/guardian.ts` -> `tests/guardian.test.ts`
  - `lib/app-flow.ts` -> `tests/app-flow.test.ts`
  - `lib/prayer.ts` -> `tests/prayer.test.ts`
  - `lib/reporting.ts` -> `tests/reporting.test.ts`
  - translation/content helpers -> `tests/content-tools.test.ts`
  - `lib/thank-you.ts` -> `tests/thank-you.test.ts`

### D5 — Translation File Location
- Primary translation strings live in `lib/translation.ts`
- `lib/constants.ts` contains language labels and product constants, not the main UI copy map

## Findings

### Build and Tests
- Check 1: PASS
  - Detail: `npm run build` completed successfully.
- Check 2: PASS
  - Detail: `npm test` ran 47 tests: 47 passed, 0 failed.

### Guardian Integrity
- Check 3: PASS
  - Detail: `METAPHOR_SHIELD_PATTERNS` exists in `lib/guardian.ts` with 14 entries.
- Check 4: PASS
  - Detail: `FULL_NAME_EXCLUSIONS` exists in `lib/guardian.ts` and includes virtue-name exclusions such as `Hope`, `Grace`, and `Faith`.
- Check 5: PASS
  - Detail: `PHONE_PATTERN` in `lib/guardian.ts` matches `09XX`, `+639XX`, and `639XX` forms via `(?:\\+?63[-\\s]?|0)9...`.
- Check 6: PASS
  - Detail: exact violent-intent lexicon entries exist in `data/harm-lexicon.json`, including Tagalog entries such as `papatayin kita` and `tatapusin kita`.
- Check 7: PASS
  - Detail: `scrubPII` exists in `lib/guardian.ts` and uses hospital aliases including `pgh`, `st lukes`, `medical city`, and `makati med`.

### Debug Artifacts
- Check 8: FAIL
  - Detail: `console.log` found at `lib/app-flow.ts:113` and `lib/posts-context.tsx:232`.
- Check 9: FAIL
  - Detail: `lib/posts-context.tsx:232` still logs realtime subscription status with `console.log("[GraceFul] Realtime status:", status);`.

### TypeScript Surface
- Check 10: FAIL
  - Detail: `: any` found at `lib/posts-context.tsx:116`, `lib/posts-context.tsx:141`, `lib/posts-context.tsx:159`, `lib/posts-context.tsx:177`, and `lib/posts-context.tsx:212`.

### Translation Coverage
- Check 11: PASS
  - Detail: supported language keys are `en`, `tl`, `ceb`, `hil`, and `es`, as declared in `types/index.ts` and `lib/constants.ts`.
- Check 12: FAIL
  - Detail: `lib/translation.ts` only defines `UI_COPY` entries for `en`, `tl`, and `ceb`. Dedicated top-level UI copy is missing for `hil` and `es` for all groups: `feed`, `emotionStep`, `categoryStep`, `messageStep`, `supportStep`, `translateStep`, `reviewStep`, `postCard`, `prayerModal`, `prayerList`, `guardianWarning`, `crisis`.
- Check 13: PASS
  - Detail: crisis copy includes NCMH `1553` and Hopeline references in `lib/guardian.ts`, `lib/translation.ts`, `lib/constants.ts`, and `lib/ai.ts`.

### Principle Violations
- Check 14: REVIEW
  - Detail: user-facing `account` copy appears at `lib/translation.ts:586`, `lib/translation.ts:724`, and `lib/translation.ts:862`.
  - Detail: no `profile`, `follow`, `rank`, `leaderboard`, or `points` matches were found.
  - Detail: `score` matches exist only in safety/internal contexts and were not flagged as social metrics.

### Realtime Safety
- Check 15: PASS
  - Detail: every realtime handler in `lib/posts-context.tsx` guards event payload access before using `new` or `old` data.
- Check 16: FAIL
  - Detail: the posts UPDATE handler in `lib/posts-context.tsx:141-150` checks only `event?.new?.id` and then syncs with `event.new.hearts ?? post.hearts`; it does not verify that the update payload explicitly contains a `hearts` field before applying sync logic.

### Dev Tooling Gate
- Check 17: FAIL
  - Detail: `components/TestDashboard.tsx:14-15` is env-gated, but `app/feed/page.tsx:19` imports it and `app/feed/page.tsx:965` renders it unconditionally in the feed route.

## Flagged for Human Review
- `lib/translation.ts:586` -> `Share anonymously. No account needed. You are safe here.`
- `lib/translation.ts:724` -> `Magbahagi nang anonymous. Walang account na kailangan. Ligtas ka rito.`
- `lib/translation.ts:862` -> `Moambit nga anonymous. Walay account nga gikinahanglan. Luwas ka diri.`

## Open Items
- Add an explicit `hearts`-field presence check in the posts realtime UPDATE handler before syncing state.
- Complete dedicated `hil` and `es` UI copy coverage, or narrow the advertised supported UI languages to match reality.
- Remove remaining runtime debug logs from `lib/posts-context.tsx` and `lib/app-flow.ts`.
- Replace `: any` usage in realtime event handlers with typed payload handling.
- Gate `TestDashboard` usage at the `/feed` integration point, not only inside the component.
