These files are archived reference prototypes.

- They are not mounted by the current app.
- The active product flow lives in `app/page.tsx`.
- Keep them only as design and implementation history unless they are explicitly revived.
